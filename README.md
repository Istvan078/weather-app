# Weather App 🌦️  
A server-rendered weather application built with **Node.js + Express**, using **Handlebars (HBS)** templates and client-side JavaScript for a smooth search experience.

It fetches geocoding data from **Mapbox** and live weather data from **Weatherstack**.

---

## ✨ Features
- 🔎 Search weather by location (city, region, country)
- 🗺️ Geocoding via Mapbox (turns a place name into coordinates)
- 🌡️ Real-time weather data via Weatherstack
- 🧩 SSR pages rendered with Handlebars (HBS)
- ⚡ Simple frontend logic with vanilla JS (fetch + DOM update)
- ✅ Friendly error handling (invalid place, API errors)

---

## 🧱 Tech Stack
- **Backend:** Node.js, Express
- **Templating / SSR:** Handlebars (HBS)
- **Frontend:** Vanilla JavaScript
- **APIs:** Mapbox (Geocoding), Weatherstack (Current Weather)
- **Styling:** SCSS

---

## 🚀 Getting Started

### 1) Install dependencies
Run command in terminal: npm install
